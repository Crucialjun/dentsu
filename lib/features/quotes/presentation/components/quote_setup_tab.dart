import 'package:flutter/material.dart';

class QuoteSetupTab extends StatelessWidget {
  const QuoteSetupTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
